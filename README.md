# wp-restart
 WP Server - Restart services

```
cd /opt
git clone git@github.com:wpnet/wp-restart.git
cd wp-restart
nano api.conf
# add your API_URL and API_KEY to the api.conf file
bash setup.sh
```
